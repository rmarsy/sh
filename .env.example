# Copy this file to .env and fill in the values for production.
# The .env file is automatically loaded by Docker Compose.

# ── SQL Server ────────────────────────────────────────────────────────────────
MSSQL_USER=Shaiya                    # Application DB user
MSSQL_PASSWORD=Shaiya123             # Application DB user password

# ── JWT ───────────────────────────────────────────────────────────────────────
JWT_KEY=CHANGE_ME_TO_A_SECURE_KEY_AT_LEAST_32_CHARS_LONG

# ── Cloudflare Turnstile ──────────────────────────────────────────────────────
TURNSTILE_SITE_KEY=YOUR_CLOUDFLARE_TURNSTILE_SITE_KEY
TURNSTILE_SECRET_KEY=YOUR_CLOUDFLARE_TURNSTILE_SECRET_KEY
TURNSTILE_ENABLED=true

# ── Launcher API key ──────────────────────────────────────────────────────────
API_KEY=CHANGE_ME_TO_A_SECURE_API_KEY

# ── Traefik / HTTPS ───────────────────────────────────────────────────────────
# Email used by Let's Encrypt for certificate renewal notices.
ACME_EMAIL=you@example.com
# Public domain names for each service (must resolve to this server's IP).
API_DOMAIN=api.yourdomain.com
WEB_DOMAIN=yourdomain.com

# ── CORS ──────────────────────────────────────────────────────────────────────
# Public URL of Sh.Web as seen by end-users' browsers (used by the API for CORS).
# Set to https://<WEB_DOMAIN> in production.
WEB_ORIGIN=https://yourdomain.com
